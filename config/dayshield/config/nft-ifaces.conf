# /etc/dayshield/config/nft-ifaces.conf — interface definitions for nftables.
# Updated automatically by the console wizard when interfaces are assigned.
# Uses loopback as safe placeholder until the wizard runs.
define WAN_IF = lo
define LAN_IF = lo
