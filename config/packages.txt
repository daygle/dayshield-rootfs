# DayShield RootFS - deterministic package list
# One package name per line.  Blank lines and lines starting with # are ignored.
#
# Core system
base-files
bash
busybox-static
ca-certificates
coreutils
git
dash
dbus
e2fsprogs
iproute2
iputils-ping
kmod
less
libpam-runtime
locales
login
passwd
procps
sudo
systemd
systemd-sysv
chrony
udev
util-linux
vim
wget
curl
zstd

# squashfs-tools is included to provide unsquashfs for the initramfs update
# hook (see config/initramfs-tools/hooks/dayshield-rootfs). The package itself
# does not install any initramfs hooks.
# NOTE: live-boot and live-config must NOT be installed here — they install
# initramfs hooks that stall the installed system at boot (the hooks timeout
# looking for a live squashfs medium and drop to an emergency shell). They are
# injected as an overlay by the dayshield-iso pipeline for live-boot only.
squashfs-tools
# Kernel - standard amd64 kernel (non-RT) for QEMU/bare-metal compatibility.
linux-image-amd64

# Networking
nftables
ethtool
kea-dhcp4-server
kea-dhcp6-server
radvd
netbase
net-tools
wireguard
wireguard-tools
ppp
pppoe
wireless-regdb

# SSH remote access
openssh-server
openssh-sftp-server

# DNS
unbound
dns-root-data

# IDS
suricata

# Firewall / threat intelligence
# crowdsec   # available via crowdsec APT repository, added post-build
# cloudflared # installed from upstream release during rootfs build

# TLS / certificates
openssl
certbot

# Disk partitioning and formatting
dosfstools
gdisk
parted

# Bootloader tooling required by installer (BIOS + UEFI)
grub2-common
grub-pc-bin
grub-efi-amd64-bin

# Utilities needed by build scripts
gzip
tar
